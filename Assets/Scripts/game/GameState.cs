namespace game {
    public enum GameState{
        Stop,
        Connecting,
        ConnectError,
        HostError,
        Disconnect,
        Waiting,
        Pause,
        Running,
        Draw,
        Win
    }
}
